<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:ignore="ContentDescription">

    <data>
        <variable name="eventItem"
            type="com.jery.starrailhelper.data.EventItem" />
        <variable name="Utils"
            type="com.jery.starrailhelper.utils.Utils" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingVertical="4dp"
        android:layout_marginVertical="4dp"
        android:background="@drawable/item_border">

        <ImageView
            android:layout_width="4dp"
            android:layout_height="match_parent"
            tools:background="@color/colorAccent"/>

        <ImageView
            android:id="@+id/ivIcon"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_marginEnd="8dp"
            tools:background="@drawable/ic_item_stellar_jade" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_weight="1">

            <TextView
                android:id="@+id/tvCode"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:fontFamily="@font/zh_cn_modify"
                android:textSize="13sp"
                android:text="@{eventItem.event}"
                android:textColor="@color/colorOnPrimary"
                tools:text="WEB EVENT" />

        </LinearLayout>
    </LinearLayout>

</layout>
